name: Pull Request to the Dev Branch

on:
  pull_request:
    types: [opened, reopened, synchronize]
    branches:
      - dev

jobs:
  lint:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - uses: actions/setup-node@v3
        id: setup-node
        with:
          node-version: current

      - name: Get node_modules cache
        uses: actions/cache@v3
        id: cache-node-modules
        with:
          path: ~/.npm
          key: ${{ runner.os }}-node-modules-${{ hashFiles('**/package-lock.json') }}-${{ steps.setup-node.outputs.version }}

      - name: Install packages
        run: npm ci
        if: steps.cache-node-modules.outputs.cache-hit != 'true'

      - name: Run linter
        run: npm run lint

  test:
    runs-on: ubuntu-latest
    needs: lint

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - uses: actions/setup-node@v3
        id: setup-node
        with:
          node-version: current

      - name: Get node_modules cache
        uses: actions/cache@v3
        id: cache-node-modules
        with:
          path: ~/.npm
          key: ${{ runner.os }}-node-modules-${{ hashFiles('**/package-lock.json') }}-${{ steps.setup-node.outputs.version }}

      - name: Install packages
        run: npm ci
        if: steps.cache-node-modules.outputs.cache-hit != 'true'

      - name: Run tests
        run: npm run test:ci
